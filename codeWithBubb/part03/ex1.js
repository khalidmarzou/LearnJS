/* write a js prog to find the number of even digits
    in an array of integers */

// My Solution :
const evenNums = (list) => list.filter((value) => value % 2 === 0);
console.log(evenNums([5, 2, 3, 4, 1, 8]));

// BOB Solution :
// sameThing
